import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saqlainap.coursetracker',
  appName: 'Course Tracker',
  webDir: 'dist'
};

export default config;
